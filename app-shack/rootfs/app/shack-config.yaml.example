# Shack Configuration (Development Mode)

# MQTT Broker Settings
mqtt_host: localhost
mqtt_port: 1883
mqtt_username: ""
mqtt_password: ""

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
log_level: INFO
